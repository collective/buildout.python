[buildout]
links-parts =
    install-links

[install-links]
recipe = collective.recipe.template
prefix = ${buildout:directory}
dollar = $
input = ${buildout:directory}/install-links.in
output = ${buildout:directory}/bin/install-links
